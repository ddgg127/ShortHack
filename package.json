{
  "name": "short-hack-line",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check dist/server/index.js && node --check dist/client/app.js",
    "test": "node --test tests/worker.test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "wrangler": "^4.131.1"
  }
}
